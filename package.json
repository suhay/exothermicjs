{
  "scripts": {
    "bootstrap": "yarn && lerna bootstrap",
    "start": "run-s bootstrap dev",
    "watch": "lerna run watch --parallel",
    "dev": "lerna run dev --parallel",
    "build": "yarn clean && lerna run build",
    "build:all": "yarn clean && lerna run build:all",
    "build-preview": "yarn build && lerna run build-preview",
    "clean": "rimraf packages/*/dist",
    "reset": "rimraf packages/*/node_modules && yarn clean && lerna run clean --yes",
    "test": "run-s build:all jest",
    "jest": "jest --no-cache",
    "publish": "run-s publish:version publish:push",
    "publish:version": "lerna version --no-push",
    "publish:push": "lerna publish from-git && git push --follow-tags",
    "publish:dry-run": "lerna version --no-git-tag-version --no-push"
  },
  "devDependencies": {
    "@babel/core": "^7.1.5",
    "@babel/plugin-proposal-object-rest-spread": "^7.0.0",
    "@babel/plugin-transform-react-jsx": "^7.0.0",
    "@babel/plugin-transform-react-jsx-self": "^7.0.0",
    "@babel/plugin-transform-react-jsx-source": "^7.0.0",
    "@babel/preset-env": "^7.1.5",
    "@babel/preset-react": "^7.0.0",
    "ajv": "^6.5.3",
    "babel-core": "^7.0.0-bridge.0",
    "babel-jest": "^23.6.0",
    "babel-loader": "^8.0.2",
    "enzyme": "^3.7.0",
    "enzyme-adapter-react-16": "^1.7.0",
    "jest": "^23.6.0",
    "lerna": "^3.4.3",
    "rimraf": "^2.6.2",
    "webpack": "^4.18.0",
    "webpack-cli": "^3.1.0",
    "webpack-node-externals": "^1.6.0",
    "yarn-run-all": "^3.1.1"
  },
  "browserslist": [
    "last 2 Chrome versions",
    "last 2 Opera versions",
    "last 2 Firefox versions",
    "last 2 Edge versions",
    "last 2 Safari versions",
    "last 2 iOS versions",
    "last 2 ChromeAndroid versions"
  ],
  "workspaces": [
    "packages/*"
  ],
  "private": true
}
